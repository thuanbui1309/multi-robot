# multi-robot

Make sure you have `Anaconda` installed. Run command below to create a new environment and install dependencies.

```
./setup.sh
```

After setting up the environment, run the command below to start the FastAPI server.

```
./run_web.sh
```